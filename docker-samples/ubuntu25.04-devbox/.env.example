# Environment variables for docker-compose
# Copy this to .env and customize if needed

# Container user configuration
# Defaults to your current user
USER=huchen
UID=1000
GID=1000

# Enable SSH server (set to "true" to enable)
ENABLE_SSH=false
