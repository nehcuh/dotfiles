# Terminal 字体配置指南

本文档说明如何配置各种终端应用使用 Hack Nerd Font 字体。

## 📋 目录

- [为什么使用 Nerd Font](#为什么使用-nerd-font)
- [自动配置](#自动配置)
- [手动配置](#手动配置)
- [验证配置](#验证配置)
- [常见问题](#常见问题)

---

## 为什么使用 Nerd Font

**Nerd Font** 是打补丁的字体，包含大量图标和符号，特别适合开发环境：

- ✅ Git 图标（分支、状态等）
- ✅ 文件类型图标
- ✅ 开发工具图标（VS Code、Docker 等）
- ✅ 编程语言图标
- ✅ 文件夹图标
- ✅ 漂亮的 Powerline 箭头

**示例**：
```
正常字体:    main  *  docker  node.js
Nerd Font:   main  * 󰡨  󰡨  
```

---

## 自动配置

### 方法 1: 使用安装脚本（推荐）

安装 dotfiles 时会自动配置：

```bash
make install
```

安装流程会：
1. 安装 Hack Nerd Font 字体
2. 创建 Terminal.app 配置文件
3. 显示配置说明

### 方法 2: 使用配置脚本

```bash
# 运行字体配置工具
~/.dotfiles/scripts/configure-terminal-font.sh

# 仅测试字体显示
~/.dotfiles/scripts/configure-terminal-font.sh --test
```

### 方法 3: 导入 Terminal 配置

```bash
# 打开 Terminal.app 配置文件
open ~/.dotfiles/stow-packs/macos/Library/Hack-Nerd-Font.terminal

# 或使用 Makefile
make font-config
```

---

## 手动配置

### Terminal.app (macOS 内置)

#### 方法 1: 导入配置文件（推荐）

1. 双击配置文件：
   ```bash
   open ~/.dotfiles/stow-packs/macos/Library/Hack-Nerd-Font.terminal
   ```

2. Terminal.app 会打开并显示 "Dotfiles - Hack Nerd Font" 配置

3. 设置为默认配置：
   - 打开 Terminal 偏好设置（⌘+,）
   - 在 "配置文件" 标签中，选择 "Dotfiles - Hack Nerd Font"
   - 点击 "默认" 按钮

#### 方法 2: 手动配置

1. 打开 Terminal.app
2. 打开偏好设置（⌘+,）
3. 选择 "配置文件" 标签
4. 点击 "+" 创建新配置，命名为 "Hack Nerd Font"
5. 在 "文本" 标签中：
   - 点击 "字体" 下的 "更改" 按钮
   - 选择 "Hack Nerd Font"（或 "Hack Nerd Font Mono"）
   - 大小设置为 13
   - 点击 "选择"
6. 在 "窗口" 标签中：
   - 设置列数：80
   - 设置行数：25
7. 设置为默认配置

### iTerm2

1. 打开 iTerm2
2. 打开偏好设置（⌘+,）
3. 选择 "Profiles" → "Text"
4. 在 "Font" 部分：
   - 点击 "Change Font"
   - 选择 "Hack Nerd Font"
   - 大小设置为 13
5. 如果需要，同样设置 "Non-ASCII Font" 为 "Hack Nerd Font Mono"
6. 设置此配置为默认（点击 "Other Actions..." → "Set as Default")

### Warp

1. 打开 Warp
2. 打开设置（⌘+,）
3. 选择 "Appearance"
4. 在 "Font" 下拉菜单中选择 "Hack Nerd Font"
5. 调整字体大小（建议 13）

### VS Code 集成终端

1. 打开 VS Code 设置（⌘+,）
2. 搜索 `terminal.integrated.fontFamily`
3. 设置为：
   ```json
   "terminal.integrated.fontFamily": "Hack Nerd Font"
   ```
4. 可选：设置字体大小
   ```json
   "terminal.integrated.fontSize": 13
   ```

---

## 验证配置

### 检查字体安装

```bash
# macOS
system_profiler SPFontsDataType | grep -i "hack nerd"

# 使用配置脚本测试
~/.dotfiles/scripts/configure-terminal-font.sh --test
```

### 测试 Nerd Font 图标

在新配置的终端中运行以下命令：

```bash
# 查看 Git 图标（应该在你的提示符中看到  仓库图标）
cd ~/.dotfiles

# 测试文件图标
echo "  󰉋 󰉔 󰉓 󰉒 󰉐"

# 测试开发工具图标
echo "        󰌛  󰏖"

# 测试编程语言图标
echo "  󰘧  󰘦    󰛘  󰟷"
```

如果这些图标显示正常，说明 Nerd Font 已正确配置。

---

## 常见问题

### Q: 字体已安装但终端不显示图标？

**A**: 确保终端配置使用的是 "Hack Nerd Font"（完整名称），而不是 "Hack" 或其他变体。

### Q: VS Code 集成终端不显示图标？

**A**: 确保 `settings.json` 中正确设置了字体：
```json
{
  "terminal.integrated.fontFamily": "Hack Nerd Font Mono",
  "terminal.integrated.fontSize": 13
}
```

### Q: 某些字符显示为方块或问号？

**A**:
1. 确认安装的是 Nerd Font 版本（包含图标）
2. 重启终端应用
3. 检查字体是否正确设置

### Q: 可以使用其他 Nerd Font 吗？

**A**: 可以！项目推荐 Hack Nerd Font，但你也可以使用：
- FiraCode Nerd Font
- JetBrainsMono Nerd Font
- MesloLGS Nerd Font
- SourceCodePro Nerd Font

只需：
1. 安装字体：`brew install --cask font-[name]-nerd-font`
2. 在终端配置中选择相应的字体

### Q: 如何调整字体大小？

**A**:
- **Terminal.app**: 偏好设置 → 配置文件 → 文本 → 字体大小
- **iTerm2**: Profiles → Text → Font → 大小
- **Warp**: Settings → Appearance → Font Size
- **VS Code**: 设置中搜索 `terminal.integrated.fontSize`

### Q: 字体在所有用户间共享吗？

**A**: 不。每个 macOS 用户需要单独配置字体和终端设置。

### Q: 卸载 dotfiles 后字体还在吗？

**A**: 是的。字体通过 Homebrew 安装在系统级别，卸载 dotfiles 不会删除字体。如需删除：
```bash
brew uninstall --cask font-hack-nerd-font
```

---

## 主题和配色方案

除了字体，你可能还想配置终端的配色方案。

### 推荐配色方案

1. **Dracula** - 流行的深色主题
2. **Nord** - 冷色调深色主题
3. **Gruvbox** - 暖色调深色主题
4. **One Dark** - 类似 Atom 的主题

### 安装配色方案

#### iTerm2
```bash
# 下载并导入配色方案
# iTerm2 → Preferences → Profiles → Colors → Color Presets → Import
```

#### Terminal.app
```bash
# Terminal.app 支持导入 .terminal 配置文件
# 配置文件中包含配色方案
```

---

## Makefile 命令

```bash
# 配置终端字体
make font-config

# 测试字体显示
make font-test

# 查看字体状态
make font-status
```

---

## 相关文档

- **[README.md](./README.md)** - 项目总览
- **[QUICKSTART.md](./QUICKSTART.md)** - 快速开始
- **[ZSH_CHEATSHEET.md](./ZSH_CHEATSHEET.md)** - Zsh 提示符配置（使用 Nerd Font 图标）

---

## 更新日志

- **2024-01-05**: 创建字体配置文档
- **2024-01-05**: 添加自动配置脚本
- **2024-01-05**: 集成到安装流程

---

**提示**: 配置完字体后，重启终端以使所有更改生效。你应该在提示符中看到漂亮的 Nerd Font 图标！
