# Personal configuration files
*.local
*.personal

# Backup files
*.bak
*.backup
*.old
*.swp
*.tmp
*~

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Log files
*.log
logs/

# Cache directories
.cache/
.npm/
.yarn/

# Dotfiles backup directories
.dotfiles-backup-*

# History files
.zsh_history
.bash_history
.history

# Sensitive and personal dotfiles (managed by stow but selectively versioned)
# Sensitive package - contains secrets, never commit
stow-packs/sensitive/**
!stow-packs/sensitive/.gitkeep
!stow-packs/sensitive/README.md
!stow-packs/sensitive/**/README.md

# Personal package - contains personal preferences (safe to commit)
# Uncomment the line below if you want to exclude personal configs from git
# stow-packs/personal/home/*
# !stow-packs/personal/home/.gitkeep

# Additional sensitive files that might accidentally be added
home/.gitconfig_local
home/.zshrc.local
home/.claude.json
home/.ssh/id_*
home/.ssh/*_rsa*
home/.ssh/*.pem
config/gh/config.yml
config/aws/credentials
config/git/config.local
*.local
*.personal
.env
*.env.local

# User-specific cache and state files
.viminfo
.z
.zcompdump*
.lesshst
.sqlite_history
.python_history
.node_repl_history
.mysql_history
.psql_history
.rediscli_history
cache.db
*.cache

# Local-only / machine-specific configs that should not be committed
stow-packs/tools/.config/nvim/
stow-packs/editor/
stow-packs/personal/.config/iterm2/
stow-packs/git/.config/git/git/

# SSH keys and config (if any accidentally placed in dotfiles)
.ssh/id_*
.ssh/*_rsa*
.ssh/*.pem
.ssh/known_hosts

# Temporary directories
/tmp/

# Neovim (AstroNvim framework and plugins are auto-downloaded)
.local/share/nvim/
.local/state/nvim/
.cache/nvim/
